# Required for the AI briefing to actually call Claude instead of erroring out.
# Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Optional — enables real token balance reads in app/api/briefing/route.js.
# Ferz already has a developer relationship with GoldRush/Covalent from prior work.
COVALENT_API_KEY=

# Needed once lib/okxDex.js is wired up for real swap quotes/execution.
OKX_API_KEY=
OKX_API_SECRET=
OKX_API_PASSPHRASE=
